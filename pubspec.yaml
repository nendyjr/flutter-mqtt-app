name: flutter_mqtt_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_bloc: 8.1.0
  cupertino_icons: 1.0.2
  mqtt_client: 10.2.0
  shared_preferences: 2.2.2
  collection: 1.18.0
  equatable: 2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
